<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <OutputType>Library</OutputType>
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
    <ImplicitUsings>disable</ImplicitUsings>
    <Nullable>disable</Nullable>
    <Description>Identity bounded context — entities. No EF Core, no ASP.NET Core Identity, no MediatR: not even the framework that Register/Login inevitably touch further out leaks in here.</Description>
  </PropertyGroup>
</Project>
